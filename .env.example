# =============================================================================
# PENNY Knowledge Core - Environment Configuration
# =============================================================================
# SECURITY WARNING: Copy this file to .env and NEVER commit .env to git!
# =============================================================================

# -----------------------------------------------------------------------------
# Gateway Configuration
# -----------------------------------------------------------------------------
GATEWAY_HOST=0.0.0.0
GATEWAY_PORT=8000
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# Fleet Configuration (The Hydra)
# Define your identity containers here. Each profile maps to a headless
# AnyType Heart container running on the internal Docker network.
# -----------------------------------------------------------------------------
# Format: FLEET_<PROFILE_NAME>_URL=http://<container-name>:<port>
FLEET_PERSONAL_URL=http://heart-personal:31009
FLEET_WORK_URL=http://heart-work:31009
FLEET_RESEARCH_URL=http://heart-research:31009

# Default profile when none is specified
DEFAULT_PROFILE=personal

# -----------------------------------------------------------------------------
# Mnemonic Phrases (12-word AnyType Recovery Phrases)
# CRITICAL SECURITY: These are your vault keys. Treat them like passwords.
# - Never log these values
# - Never commit them to version control
# - Consider using a secrets manager (1Password CLI, Doppler, etc.)
# -----------------------------------------------------------------------------
MNEMONIC_PERSONAL=word1 word2 word3 word4 word5 word6 word7 word8 word9 word10 word11 word12
MNEMONIC_WORK=word1 word2 word3 word4 word5 word6 word7 word8 word9 word10 word11 word12
MNEMONIC_RESEARCH=word1 word2 word3 word4 word5 word6 word7 word8 word9 word10 word11 word12

# -----------------------------------------------------------------------------
# Redis Configuration (for distributed locking)
# -----------------------------------------------------------------------------
REDIS_URL=redis://redis:6379/0

# -----------------------------------------------------------------------------
# AnyType API Configuration
# -----------------------------------------------------------------------------
# Timeout for API calls to Heart containers (milliseconds)
ANYTYPE_TIMEOUT_MS=30000

# Retry configuration for transient failures
ANYTYPE_MAX_RETRIES=3
ANYTYPE_RETRY_DELAY_MS=500

# Delay between batch operations to avoid overwhelming single-threaded gRPC
ANYTYPE_BATCH_DELAY_MS=50

# -----------------------------------------------------------------------------
# UI Configuration (Phase 4: Chainlit)
# -----------------------------------------------------------------------------
UI_HOST=0.0.0.0
UI_PORT=8080

# -----------------------------------------------------------------------------
# LLM API Keys (at least one required for full conversational capabilities)
# Without these, the UI will use pattern-matching for basic operations
# -----------------------------------------------------------------------------
# Anthropic Claude (recommended)
ANTHROPIC_API_KEY=

# OpenAI GPT-4 (alternative)
OPENAI_API_KEY=

# -----------------------------------------------------------------------------
# Development Settings
# -----------------------------------------------------------------------------
# Enable debug mode (additional logging, no auth required)
DEBUG=false

# Enable hot reload for development
RELOAD=false
